name: unicompanion
description: Student University & Academic Companion — FSC to Graduation.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0
  flutter: ^3.24.0

dependencies:
  flutter:
    sdk: flutter

  # ── UI ──
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1
  flutter_svg: ^2.0.10+1
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  flutter_animate: ^4.5.0
  gap: ^3.0.1
  icons_plus: ^5.0.0

  # ── State Management ──
  flutter_riverpod: ^2.5.1

  # ── Navigation ──
  go_router: ^14.2.7

  # ── Backend / Supabase ──
  supabase_flutter: ^2.7.0

  # ── Auth ──
  google_sign_in: ^6.2.1

  # ── Local Storage ──
  shared_preferences: ^2.3.2

  # ── Models / Serialization ──
  freezed_annotation: ^2.4.4
  json_annotation: ^4.9.0
  equatable: ^2.0.5

  # ── Utils ──
  intl: ^0.19.0
  url_launcher: ^6.3.0
  connectivity_plus: ^6.0.5
  logger: ^2.4.0
  uuid: ^4.5.1
  fl_chart: ^0.69.0
  percent_indicator: ^4.2.3
  table_calendar: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.12
  freezed: ^2.5.7
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true